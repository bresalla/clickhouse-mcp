apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mcp-clickhouse.fullname" . }}
  labels:
    {{- include "mcp-clickhouse.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "mcp-clickhouse.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "mcp-clickhouse.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: mcp-clickhouse
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.container.port }}
              protocol: TCP
          env:
            - name: CLICKHOUSE_HOST
              value: "{{ .Values.clickhouse.host }}"
            - name: CLICKHOUSE_PROTOCOL
              value: "{{ .Values.clickhouse.protocol }}"
            - name: CLICKHOUSE_PORT
              value: "{{ .Values.clickhouse.port | toString }}"
            - name: CLICKHOUSE_SECURE
              value: "{{ .Values.clickhouse.secure | toString }}"
            - name: CLICKHOUSE_VERIFY
              value: "{{ .Values.clickhouse.verify | toString }}"
            - name: CLICKHOUSE_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.clickhouse.secret.name }}
                  key: {{ .Values.clickhouse.secret.userKey }}
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.clickhouse.secret.name }}
                  key: {{ .Values.clickhouse.secret.passwordKey }}
            - name: CLICKHOUSE_MCP_AUTH_DISABLED
              value: "{{ .Values.server.authDisabled | toString }}"
            - name: CLICKHOUSE_MCP_SERVER_TRANSPORT
              value: "{{ .Values.server.transport }}"
            - name: CLICKHOUSE_MCP_BIND_HOST
              value: "{{ .Values.server.bindHost }}"
            - name: CLICKHOUSE_MCP_BIND_PORT
              value: "{{ .Values.server.bindPort | toString }}"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 45
            timeoutSeconds: 5
            periodSeconds: 20
            failureThreshold: 6